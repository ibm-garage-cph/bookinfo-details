apiVersion: tekton.dev/v1alpha1
kind: TaskRun
metadata:
  generateName: build-push-nodejs-run-
spec:
  taskRef:
    name: build-push-nodejs
  inputs:
    resources:
      - name: git
        resourceRef:
          name: git-bookinfo-details
      - name: image
        resourceRef:
          name: dockerhub-bookinfo-details
